version: 1
formatters:
  simple:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: simple
    stream: ext://sys.stdout
loggers:
  infoLogger:
    level: DEBUG
    handlers: [console]
    propagate: no
  debugLogger:
    level: Info
    handlers: [console]
    propagate: no
  apiLogger:
    level: debug
    handlers: [console]
    propagate: no
root:
  level: DEBUG
  handlers: [console]